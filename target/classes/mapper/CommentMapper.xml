<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookrecommend.dao.CommentDao">
    <select id="getCommentList" resultType="Comment">
        SELECT c.c_id,b.book_name,u.username,c.score,c.time,c.comment,c.approval FROM comment c,books b,user u
        WHERE 1=1 AND c.book_id = b.book_id AND c.u_id = u.u_id
        <if test="c_id != null and c_id != ''">
            AND c.c_id like concat('%',#{c_id},'%')
        </if>
        <if test="book_name != null and book_name != ''">
            AND b.book_name like concat('%',#{book_name},'%')
        </if>
        <if test="username != null and username != ''">
            AND u.username like concat('%',#{username},'%')
        </if>
        <if test="comment != null and comment != ''">
            AND c.comment like concat('%',#{comment},'%')
        </if>
        <if test="book_id != null">
            AND c.book_id = #{book_id}
        </if>
        limit #{currentPage},#{pageSize}
    </select>

    <select id="totalCount" resultType="Integer">
        SELECT COUNT(c.c_id) FROM comment c,books b,user u
        WHERE 1=1 AND c.book_id = b.book_id AND c.u_id = u.u_id
        <if test="c_id != null and c_id != ''">
            AND c.c_id like concat('%',#{c_id},'%')
        </if>
        <if test="book_name != null and book_name != ''">
            AND b.book_name like concat('%',#{book_name},'%')
        </if>
        <if test="username != null and username != ''">
            AND u.username like concat('%',#{username},'%')
        </if>
        <if test="comment != null and comment != ''">
            AND c.comment like concat('%',#{comment},'%')
        </if>
        <if test="book_id != null">
            AND c.book_id = #{book_id}
        </if>
    </select>

    <delete id="delComment">
        DELETE FROM comment WHERE c_id = #{c_id}
    </delete>

    <delete id="deleteSelectedComment" parameterType="String">
        DELETE FROM comment WHERE c_id IN (${ck})
    </delete>

    <update id="approve" parameterType="Integer">
        UPDATE comment SET approval = approval+1 WHERE c_id = #{c_id}
    </update>

    <insert id="addComment">
        INSERT INTO comment(book_id, u_id, time, comment)
        VALUE (#{book_id}, #{u_id}, now(), #{comment})
    </insert>

    <insert id="addRating" parameterType="Ratings">
        INSERT INTO ratings(ID, ISBN, Rating)
        VALUE (#{ID}, #{ISBN}, #{Rating})
    </insert>
</mapper>